import React from 'react';
import loading from '../assets/loading - logo.gif';

const Loading = () => {
  return (
    <div className='loading'>
        <img src={loading} alt="logo-loading" />
    </div>
  );
};

export default Loading;
